[project]
name = "shiny-pokemon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.3.1",
    "numpy>=2.0.0",
    "PySide6>=6.7.0",
    "plotly>=6.6.0",
    "pytest>=9.0.2",
]

[project.scripts]
shiny-cli = "shiny_pokemon.cli:main"

[project.gui-scripts]
shiny-gui = "shiny_pokemon.gui.app:main"
